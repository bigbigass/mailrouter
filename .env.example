# Local configuration lives in config/app.config.json.
# Copy config/app.config.example.json to config/app.config.json and fill in every value there.
#
# These environment variables are optional overrides for deployment platforms.
# Prisma CLI still reads DATABASE_URL directly; use `npm run prisma:*` scripts so it is generated from config automatically.
